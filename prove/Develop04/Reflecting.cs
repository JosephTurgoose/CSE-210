using System;

class Reflecting : Activity
{
    public Reflecting(string activityName, string description) : base(activityName, description)
    {
        
    }
    
    public void Play()
    {
        // Reflective Prompt
        // Wait
        // Follow-up
        // Wait
        // Continue follow-up questions and waiting until out of questions to follow up with
    }
}